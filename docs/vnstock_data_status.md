# vnstock_data Status

## Current status
- `vnstock_data` is not available in the current environment.
- Unified UI testing is skipped for now.

## Working decision
- Keep the existing scripts that run with classic `vnstock`.
- Continue using the current working flow until `vnstock_data` becomes available.

## Note
- This is an environment limitation, not a logic error in the current scripts.
