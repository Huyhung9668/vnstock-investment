# Daily Run

1. Kiem tra `.env`, `config/`, `data/derived/`.
2. Chay `scripts/run_trading_terminal.py`.
3. Doi chieu `run_id`, `manifest_path`, `output_dir`.
4. Mo `market_analysis_report.md` de review tong hop.
