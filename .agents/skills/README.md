# Agent Skills

This folder is reserved for agent-specific rules, skills, and reusable guidance.

## Purpose
- Keep the main agent behavior consistent.
- Add focused instructions only when the workflow needs them.
- Prefer small, readable skill files over large monolithic rules.

## Suggested future files
- `investing.md` for analysis guardrails.
- `reporting.md` for report style rules.
- `data_access.md` for Vnstock usage conventions.
